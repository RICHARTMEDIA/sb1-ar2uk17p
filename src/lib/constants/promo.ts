import type { SectionVisibility } from '../types/promo';

export const DEFAULT_SECTIONS: SectionVisibility = {
  latestRelease: true,
  platformLinks: true,
  otherReleases: true,
  socialLinks: true
};